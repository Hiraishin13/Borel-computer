export default function AdminCustomersPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Clients</h1>
      <p className="mt-4 text-sm text-muted">
        Liste des clients, historique d&apos;achat et segmentation — à implémenter via
        <code className="mx-1 rounded bg-secondary px-1">GET /api/admin/customers</code>.
      </p>
    </div>
  )
}
